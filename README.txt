FIXED DCL PRISM OVERLAY (server-side proxy version)

This version fixes the browser CORS error by calling DCL from a Netlify Function.

Deploy with Git integration or Netlify CLI (not plain drag-and-drop if functions are not built).

After deployment, use ONE link in PRISM:
https://YOUR-SITE.netlify.app/?match=5923

For later matches, only change the match number.
